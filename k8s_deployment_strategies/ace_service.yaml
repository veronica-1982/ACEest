apiVersion: v1
kind: Service

metadata:
  name: aceest-service

spec:
  type: NodePort

  selector:
    app: aceest

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080